# Sprint 1 — Bug Fixes + Installer Hardening
## 2026-06-02 · Branch: `feature/sprint-1-installer-hardening`

**Status:** In Progress  
**Base commit:** `b26eb3bd` (next-gen)  
**Council review:** APPROVED — see `Council_Review_ProjectWide_2026-06-02.md`

---

## Sprint Goal

Fix all Critical and High bugs found in the project-wide council review, plus key Medium items that are small-scope. Make llama.cpp usable end-to-end from a fresh install. Add opt-in bearer token auth for NodeWorker. Rename solution file per Decision D4.

---

## Tasks In Scope

| Key | Priority | Title |
|-----|----------|-------|
| TSK-0001 | Critical | Wire LlamaCppInferenceNode into Mcp + Dashboard dispatch |
| TSK-0002 | Critical | Add -SkipDotNet/-SkipOllama/-SkipModels params to deploy scripts |
| TSK-0003 | High | Fix CurStepChanged CBackend collapse (installer bug) |
| TSK-0004 | High | Fix `$env:($Name)` bug in setup-node-b.ps1 |
| TSK-0005 | Medium | Add UseWindowsService() to NodeWorker/Program.cs |
| TSK-0006 | Medium | Fix queue capacity hardcoded by NodeId string |
| TSK-0007 | Medium | Add Worker resilience pipeline in Dashboard |
| TSK-0008 | Medium | Fix stale pinned fallback tag in Setup-LlamaCpp.ps1 |
| TSK-0009 | Medium | Fix $extractOk logic bug in Setup-LlamaCpp.ps1 |
| TSK-0010 | High | Add model path input to installer wizard |
| TSK-0011 | Medium | Add CUDA build version selector to installer |
| TSK-0012 | Low | Rename Orchestrator.slnx → SplitBrain.slnx |
| TSK-0013 | High | Add opt-in bearer token auth to NodeWorker (safe default: off) |
| TSK-0014 | Low | Remove redundant CopilotSdk arm in Dashboard dispatch |
| TSK-0015 | Low | Fix PrepareToInstall prereq check (inline instead of external script) |
| TSK-0016 | Low | Add $LASTEXITCODE check after ollama pull in setup-node-b.ps1 |

---

## Files Changed

| File | Changes |
|------|---------|
| `installer/SplitBrainAI.iss` | Fix CBackend collapse (TSK-0003), model path field (TSK-0010), CUDA selector (TSK-0011), prereq inline (TSK-0015), RegisterStartup task |
| `installer/scripts/Setup-LlamaCpp.ps1` | Fix fallback tag (TSK-0008), fix $extractOk (TSK-0009), accept -ModelPath and -CudaBuild |
| `deploy/setup-node-a.ps1` | Add SkipDotNet/SkipOllama/SkipModels params (TSK-0002), add -UseBasicParsing |
| `deploy/setup-node-b.ps1` | Add skip params (TSK-0002), fix $env:($Name) (TSK-0004), add LASTEXITCODE check (TSK-0016) |
| `src/Orchestrator.NodeWorker/Program.cs` | Add UseWindowsService() (TSK-0005), add opt-in auth (TSK-0013) |
| `src/Orchestrator.Mcp/Program.cs` | Add LlamaCpp dispatch (TSK-0001), fix queue capacity (TSK-0006) |
| `src/SplitBrain.Dashboard/Program.cs` | Add LlamaCpp dispatch (TSK-0001), Worker resilience (TSK-0007), fix queue capacity (TSK-0006), remove redundant arm (TSK-0014) |
| `SplitBrain.slnx` (new) | Solution rename (TSK-0012) |
| `Orchestrator.slnx` | Add deprecation comment, points to SplitBrain.slnx |

---

## Deferred to Sprint 2

- TSK: NodeWorker OpenTelemetry
- TSK: `NodeCInferenceNode.CreateAsync` blocking startup (refactor to hosted service)
- TSK: `ApplyPatchTool` path-scope enforcement audit
- TSK: Full project-prefix rename (Orchestrator.* → SplitBrain.*)
- TSK: HTTPS enforcement / reverse proxy documentation

---

## Acceptance Criteria

- [ ] `dotnet build Orchestrator.slnx` passes (or `SplitBrain.slnx`)
- [ ] `dotnet test` — all 221+ tests pass
- [ ] Fresh installer run (Orchestrator + Worker, llama.cpp Native) completes without PowerShell errors
- [ ] `nodes.json` with `"Provider": "LlamaCpp"` no longer crashes Mcp or Dashboard
- [ ] NodeWorker service stops cleanly via SCM
- [ ] Council review of implementation: PASS or CONDITIONAL PASS
